<!-- API 层：只负责 HTTP 入参出参、路由和校验，不直接操作数据库。 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.yonng</groupId>
        <artifactId>agent-backend-java-parent</artifactId>
        <version>1.1.0</version>
    </parent>

    <artifactId>agent-api</artifactId>
    <name>agent-api</name>

    <dependencies>
        <dependency>
            <groupId>com.yonng</groupId>
            <artifactId>agent-application</artifactId>
        </dependency>

        <!-- API 模块只声明编译所需的 Web 注解和 HTTP 类型，Web 容器由启动层提供。 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Spring Security Web 提供 OncePerRequestFilter、SecurityContextHolder 等。 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>

        <!-- Spring Security Config 提供 @EnableWebSecurity、SecurityFilterChain 等。 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
        </dependency>

        <!-- WebMvcConfigurer、HandlerMethodArgumentResolver 等 Spring MVC 编译依赖。 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
        </dependency>
    </dependencies>
</project>
